# ScriptGraph 数据卡

## 数据集概览

ScriptGraph 的公开数据完全由本项目原创生成，用于复现剧本检索、连续性检查和改稿影响分析。数据不含商业剧本、公司内部资料、真实个人信息或受限内容。

| 组成 | 数量 | 用途 |
|---|---:|---|
| 中文合成剧本 | 4 部 / 120 场 | 解析、实体关系抽取与图谱构建 |
| 单场事实问题 | 60 | 检查局部事实召回 |
| 跨场多跳问题 | 60 | 检查图扩展与跨场关系召回 |
| 时间或状态问题 | 28 | 检查事件顺序与状态变化 |
| 无答案问题 | 32 | 检查证据门禁与拒答 |
| 连续性冲突 | 48 | 检查称谓、知情、道具归属与时序冲突 |

每部剧本包含 30 场、45 条问题与 12 处冲突。两个剧本进入公共固定样例 Demo，四个剧本均进入离线评测。

## 数据格式与溯源

- 剧本使用 Fountain 文本格式；场景、人物、地点、道具、事件和事实保留原始行号。
- 问题记录包含项目、问题类型、问题文本、相关场次和复核状态。
- 冲突记录包含冲突类别、摘要、两侧场次和复核状态。
- 图谱实体与关系由解析器从同一份剧本生成，不使用模型常识补写缺失事实。

## 构建与质量控制

数据生成使用固定随机种子和可复现模板。自动检查覆盖数量、ID 唯一性、场次引用、项目归属、关系端点、冲突类别分布和评测可运行性。当前关键样本仍标记为 `pending_user_review`；在作者逐条接受前，仓库中的指标保持 `provisional`。

人工复核入口：

```bash
python scripts/review_dataset.py --kind questions --reviewer YOUR_NAME
python scripts/review_dataset.py --kind conflicts --reviewer YOUR_NAME
```

复核工具会断点保存结论，不会覆盖剧本文本。

## 适用范围

该数据适合：

- 对比 BM25、向量、混合检索和图扩展的可追溯召回表现。
- 验证无答案拒答、一致性候选和改稿影响路径。
- 演示 Neo4j / FAISS 可插拔后端及 FastAPI 产品接口。

不适合：

- 估计真实影视项目中的错误率、编剧效率或商业收益。
- 训练通用剧本生成模型。
- 代表不同题材、语言、写作习惯或真实制片流程的完整分布。

## 已知局限

- 剧本与问题具有合成数据的规则性，离线指标可能高于真实长剧本。
- 当前评测重点是检索与证据定位，不衡量开放式回答的文学质量。
- 连续性冲突是受控注入，尚未覆盖隐含动机、复杂视角和多人协作造成的软冲突。
- 当前字符向量基线用于零密钥复现，不等同于生产级语义嵌入模型。

## 许可

生成剧本和评测数据使用 CC BY 4.0；代码使用 Apache-2.0。引用或再分发时请保留来源说明。
